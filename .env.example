# Copy this file to .env.local and fill in the keys.

# --- Client-safe (bundled into the app) ---
VITE_SUPABASE_URL=https://avgoopcemvczznwvslkn.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key-here

# --- Server-only, used ONLY by scripts/seed.mjs ---
# Settings → API → service_role. NEVER prefix with VITE_ and never commit it.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
